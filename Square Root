def is_square(n):    
    if n<1:
        return False
    else:
        for i in range(int(n/2)+1):
            if (i*i)==n:
                return True
    return False 
    
    #having problems testing that 0 is a square number
